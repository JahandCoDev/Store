apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin
  namespace: ecom
  labels:
    app: admin
    tags.datadoghq.com/env: "production-store"
    tags.datadoghq.com/service: "store-admin"
    tags.datadoghq.com/version: "latest"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: admin
  template:
    metadata:
      labels:
        app: admin
        admission.datadoghq.com/enabled: "true"
        tags.datadoghq.com/env: "production-store"
        tags.datadoghq.com/service: "store-admin"
        tags.datadoghq.com/version: "latest"
      annotations:
        ad.datadoghq.com/admin.logs: '[{"source": "nodejs", "service": "store-admin"}]'
    spec:
      imagePullSecrets:
      - name: registry-credentials
      containers:
      - name: admin
        image: registry.jahandco.dev/store/admin:latest
        imagePullPolicy: Always
        command: ["sh", "-lc"]
        args:
          - "npx prisma db execute --config prisma.config.ts --file prisma/sql/ensure_user_displayid_seq.sql && npx prisma db push --accept-data-loss --config prisma.config.ts && npm start"
        ports:
        - containerPort: 3000
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: postgres-secret
              key: DATABASE_URL
        - name: S3_ENDPOINT
          valueFrom:
            secretKeyRef:
              name: minio-s3-secret
              key: S3_ENDPOINT
        - name: S3_REGION
          valueFrom:
            secretKeyRef:
              name: minio-s3-secret
              key: S3_REGION
        - name: S3_BUCKET
          valueFrom:
            secretKeyRef:
              name: minio-s3-secret
              key: S3_BUCKET
        - name: S3_ACCESS_KEY_ID
          valueFrom:
            secretKeyRef:
              name: minio-s3-secret
              key: S3_ACCESS_KEY_ID
        - name: S3_SECRET_ACCESS_KEY
          valueFrom:
            secretKeyRef:
              name: minio-s3-secret
              key: S3_SECRET_ACCESS_KEY
        - name: S3_FORCE_PATH_STYLE
          valueFrom:
            secretKeyRef:
              name: minio-s3-secret
              key: S3_FORCE_PATH_STYLE
        - name: S3_PUBLIC_BASE_URL
          valueFrom:
            secretKeyRef:
              name: minio-s3-secret
              key: S3_PUBLIC_BASE_URL
        - name: NEXTAUTH_SECRET
          valueFrom:
            secretKeyRef:
              name: postgres-secret
              key: NEXTAUTH_SECRET
        - name: NEXTAUTH_URL
          value: "https://admin.jahandco.dev"
        - name: QUOTE_SIGNATURE_IMAGE_URL
          value: "https://jahandco.net/signature.PNG"
        - name: NODE_ENV
          value: "production"
        - name: DD_ADMIN_APP_TOKEN
          valueFrom:
            secretKeyRef:
              name: postgres-secret
              key: DD_ADMIN_APP_TOKEN
              optional: true
        - name: DD_ENV
          value: "production-store"
        - name: DD_SERVICE
          value: "store-admin"
        - name: DD_VERSION
          value: "latest"

        envFrom:
        - secretRef:
            name: postgres-secret
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "500m"
            memory: "512Mi"

---
apiVersion: v1
kind: Service
metadata:
  name: admin-svc
  namespace: ecom
spec:
  selector:
    app: admin
  ports:
  - name: http
    protocol: TCP
    port: 80
    targetPort: 3000

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: admin-ingress
  namespace: ecom
  annotations:
    traefik.ingress.kubernetes.io/router.entrypoints: websecure
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - admin.jahandco.dev
      secretName: admin-tls
  rules:
    - host: admin.jahandco.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: admin-svc
                port:
                  number: 80



